#include "monty.h"

/**
 * nop_stack - function that does not do any thing!
 * @stack: pointer to the stack.
 * @line_number: line number.
 *
 * Return: void.
 */
void nop_stack(stack_t **stack, unsigned int line_number)
{
(void)stack;
(void)line_number;
}
